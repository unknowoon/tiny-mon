# Quekka

Kafka 스타일 단일 노드 메시지 큐 시스템 (queue + Kafka)

## 프로젝트 개요

**목표**: Kafka 스타일의 단일 노드 메시지 큐 시스템 구현
**난이도**: 중상급

Apache Kafka의 핵심 개념을 학습하고, 이를 C/C++로 직접 구현하며 메시지 큐 시스템의 내부 동작을 이해하는 것을 목표로 합니다.

## 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         Quekka Server                           │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  Producer   │  │  Producer   │  │  Consumer   │   Clients    │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
│         │                │                │                     │
├─────────┴────────────────┴────────────────┴─────────────────────┤
│                      TCP Connection Layer                       │
│                    (epoll + non-blocking I/O)                   │
├─────────────────────────────────────────────────────────────────┤
│                      Protocol Parser Layer                      │
│              (Command parsing, Request/Response)                │
├─────────────────────────────────────────────────────────────────┤
│                       Broker Core Layer                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    Topic Manager                        │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐     │    │
│  │  │ Topic: logs │  │Topic: events│  │Topic: metrics│     │    │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬───────┘     │    │
│  └─────────┴────────────────┴────────────────┴─────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│                      Storage Engine Layer                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                   Segment Manager                       │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐               │    │
│  │  │SegmentLog│  │SegmentIdx│  │ Offset   │               │    │
│  │  │ (.log)   │  │ (.idx)   │  │ Manager  │               │    │
│  │  └──────────┘  └──────────┘  └──────────┘               │    │
│  └─────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│                         File System                             │
│                    (Append-only Logs + Index)                   │
└─────────────────────────────────────────────────────────────────┘
```

## 프로젝트 구조

```
quekka/
├── CMakeLists.txt
├── README.md
├── Dockerfile
├── shl/
│   ├── build.sh
│   └── clean.sh
│
├── libsrc/
│   ├── libLogger.c               # 로깅 라이브러리
│   └── libLogger.h
│
├── src/
│   ├── main.cpp                  # 서버 진입점
│   │
│   ├── network/                  # 네트워크 레이어
│   │   ├── tcp_server.h          # epoll 기반 TCP 서버
│   │   ├── tcp_server.c
│   │   ├── connection.h          # 클라이언트 연결 관리
│   │   └── connection.c
│   │
│   ├── protocol/                 # 프로토콜 레이어
│   │   ├── command.h             # 명령어 정의
│   │   ├── parser.h              # 프로토콜 파서
│   │   ├── parser.c
│   │   ├── request.h             # 요청 구조체
│   │   └── response.h            # 응답 구조체
│   │
│   ├── broker/                   # 브로커 코어
│   │   ├── broker.h              # 메인 브로커 로직
│   │   ├── broker.c
│   │   ├── topic.h               # 토픽 관리
│   │   ├── topic.c
│   │   ├── consumer_group.h      # 컨슈머 그룹
│   │   └── consumer_group.c
│   │
│   ├── storage/                  # 스토리지 엔진
│   │   ├── segment.h             # 세그먼트 파일
│   │   ├── segment.c
│   │   ├── index.h               # 인덱스 파일
│   │   ├── index.c
│   │   ├── message.h             # 메시지 포맷
│   │   └── retention.c           # 보존 정책
│   │
│   └── utils/                    # 유틸리티
│       ├── buffer.h              # 버퍼 관리
│       ├── buffer.c
│       ├── crc32.h               # 체크섬
│       └── crc32.c
│
├── client/                       # 테스트용 클라이언트
│   ├── producer.c                # Producer CLI
│   └── consumer.c                # Consumer CLI
│
├── data/                         # 런타임 데이터 (gitignore)
│   └── topics/
│       └── <topic_name>/
│           ├── 00000000000000000000.log
│           ├── 00000000000000000000.idx
│           └── consumer_offsets/
│               └── <group_id>.offset
│
└── tests/
    ├── test_segment.c
    ├── test_parser.c
    └── test_broker.c
```

## 프로토콜 명세

### 기본 형식

```
<COMMAND> <args...>\r\n
[payload if needed]
```

### 명령어

| 명령어      | 설명                            | 형식                                          |
|-------------|---------------------------------|-----------------------------------------------|
| `PUBLISH`   | 토픽에 메시지 발행              | `PUBLISH <topic> <length>\r\n<payload>`       |
| `SUBSCRIBE` | 토픽 구독 (Consumer Group 등록) | `SUBSCRIBE <topic> <group_id>\r\n`            |
| `POLL`      | 메시지 폴링                     | `POLL <topic> <group_id> [max] [timeout]\r\n` |
| `COMMIT`    | Offset 커밋                     | `COMMIT <topic> <group_id> <offset>\r\n`      |
| `CREATE`    | 토픽 생성                       | `CREATE <topic>\r\n`                          |
| `DELETE`    | 토픽 삭제                       | `DELETE <topic>\r\n`                          |
| `TOPICS`    | 토픽 목록                       | `TOPICS\r\n`                                  |
| `INFO`      | 토픽 정보                       | `INFO <topic>\r\n`                            |
| `STATS`     | 서버 상태                       | `STATS\r\n`                                   |
| `QUIT`      | 연결 종료                       | `QUIT\r\n`                                    |

### 응답 형식

```
+OK <data>\r\n              # 성공
-ERR <error_message>\r\n    # 실패
+MESSAGES <count>\r\n       # 메시지 목록
+EMPTY\r\n                  # 메시지 없음
```

## 빠른 시작

### 요구사항

- CMake 3.10+
- C++17 지원 컴파일러
- pthread

### 빌드 및 실행

```bash
# 빌드
./shl/build.sh

# 서버 실행
./bin/quekka

# 클라이언트 연결 테스트 (다른 터미널에서)
nc localhost 9999
```

### Docker

```bash
docker build -t quekka:latest .
docker run -p 9999:9999 --name quekka quekka:latest
```

### 사용 예시

```bash
# 터미널 1: 서버 시작
./bin/quekka --port 9999

# 터미널 2: Producer
echo -e "CREATE logs\r" | nc localhost 9999
echo -e "PUBLISH logs 5\r\nhello" | nc localhost 9999

# 터미널 3: Consumer
echo -e "SUBSCRIBE logs group1\r" | nc localhost 9999
echo -e "POLL logs group1 10\r" | nc localhost 9999
echo -e "COMMIT logs group1 0\r" | nc localhost 9999
```

## 성능 목표

| 지표            | 목 표            |
|-----------------|------------------|
| Producer 처리량 | > 10,000 msg/sec |
| Consumer 처리량 | > 20,000 msg/sec |
| 지연 시간 (P99) | < 10ms           |
| 메시지 크기     | 1KB 기준         |

## 개발 로드맵

### 마일스톤 1: 인메모리 메시지 큐

**Week 1: 프로젝트 셋업 + 프로토콜 설계**
- [ ] `src/protocol/command.h` - 명령어 enum, 상수 정의
- [ ] `src/storage/message.h` - 메시지 헤더 구조체 (offset, payload_size, crc32, timestamp)
- [ ] `src/utils/crc32.c` - 체크섬 유틸리티 구현

**Week 2: epoll 서버 완성**
- [ ] `src/network/tcp_server.c` - epoll 기반 비동기 TCP 서버
- [ ] `src/network/connection.c` - 클라이언트 연결 관리 (recv/send 버퍼)
- [ ] 에코 서버 동작 확인 (`nc localhost 9999`)

**Week 3: 프로토콜 파서**
- [ ] `src/protocol/parser.c` - 명령어 파싱 (PUBLISH, POLL 등)
- [ ] `src/protocol/request.h` - 파싱된 요청 구조체
- [ ] `src/protocol/response.c` - 응답 포맷팅 (+OK, -ERR)

**Week 4: 토픽 + 메모리 큐**
- [ ] `src/broker/topic.c` - 토픽 CRUD (CREATE, DELETE, TOPICS)
- [ ] `src/broker/broker.c` - PUBLISH/POLL 처리
- [ ] Ring buffer 기반 인메모리 저장소

### 마일스톤 2: 영속화 + Consumer Group

**Week 5: 세그먼트 저장**
- [ ] `src/storage/segment.c` - append-only 로그 쓰기
- [ ] offset 기반 메시지 읽기 (pread)
- [ ] 세그먼트 롤오버 (100MB 초과 시 새 파일)

**Week 6: 인덱스 + 빠른 검색**
- [ ] `src/storage/index.c` - sparse index 쓰기 (매 100개 메시지)
- [ ] mmap 기반 인덱스 읽기
- [ ] 이진 탐색으로 offset → position 변환

**Week 7: Consumer Group + Offset**
- [ ] `src/broker/consumer_group.c` - 그룹 관리
- [ ] offset 파일 저장/복구 (`consumer_offsets/<group>.offset`)
- [ ] SUBSCRIBE/COMMIT 명령어 연결

### 마일스톤 3: 안정화

**Week 8: Retention + 정리**
- [ ] `src/storage/retention.c` - 시간/크기 기반 세그먼트 삭제
- [ ] 백그라운드 정리 스레드
- [ ] graceful shutdown (SIGINT/SIGTERM 처리)

**Week 9: 테스트 + 클라이언트**
- [ ] `client/producer.c` - 명령줄 Producer
- [ ] `client/consumer.c` - 명령줄 Consumer
- [ ] 벤치마크 (처리량 측정)

---

## 클라이언트 라이브러리 학습 (libquekka)

> 📚 **학습 목표**: 토픽 발행/구독 기능을 다른 개발자에게 라이브러리 형태로 제공하는 방법을 학습합니다.

### 학습 방식

**API-First 설계**: 사용자 관점의 예제 코드를 먼저 작성하고, 해당 기능을 역으로 구현합니다.

```
1. 사용자가 원하는 코드 형태 상상 → 예제 작성
2. 예제가 컴파일되도록 헤더 파일 작성
3. 헤더에 맞춰 실제 구현
4. 테스트 및 개선
```

### 학습 단계

| 단계 | 주 제                                | 문서                                                                   |
|------|--------------------------------------|------------------------------------------------------------------------|
| 1    | API 설계 - 사용자 관점의 인터페이스  | [docs/01-api-design.md](docs/01-api-design.md)                         |
| 2    | 프로토콜 직렬화 - 명령어를 바이트로  | [docs/02-protocol-serialization.md](docs/02-protocol-serialization.md) |
| 3    | 연결 관리 - TCP 클라이언트 구현      | [docs/03-connection-management.md](docs/03-connection-management.md)   |
| 4    | 에러 처리 - 견고한 라이브러리 만들기 | [docs/04-error-handling.md](docs/04-error-handling.md)                 |
| 5    | 메모리 관리 - 버퍼와 생명주기        | [docs/05-memory-management.md](docs/05-memory-management.md)           |
| 6    | 통합 및 테스트                       | [docs/06-integration-testing.md](docs/06-integration-testing.md)       |

### 최종 결과물

```
libquekka/
├── include/
│   └── quekka.h          # 공개 API (사용자에게 노출)
├── src/
│   ├── quekka.c          # API 구현
│   ├── protocol.c        # 프로토콜 직렬화/파싱
│   ├── connection.c      # TCP 연결 관리
│   └── internal.h        # 내부 헤더
├── examples/
│   ├── publisher.c       # 발행자 예제
│   └── subscriber.c      # 구독자 예제
└── CMakeLists.txt
```

---

## 확장 가능성 (향후)

| 기능        | 설 명               |
|-------------|---------------------|
| Partition   | 병렬 처리           |
| Batch 처리  | 여러 메시지 한 번에 |
| 압축        | LZ4/Snappy          |
| TLS         | 암호화 통신         |
| 웹 대시보드 | 모니터링 UI         |
| 복제        | 고가용성            |

## 참고 자료

- [Kafka: The Definitive Guide](https://www.confluent.io/resources/kafka-the-definitive-guide/)
- [Kafka Log Internals](https://kafka.apache.org/documentation/#log)
- The Linux Programming Interface (Michael Kerrisk)
- [Redpanda](https://github.com/redpanda-data/redpanda) - Kafka 호환 C++

## 라이선스

이 프로젝트는 교육 및 학습 목적으로 제작되었습니다.
